$('#edit-clock-entry-<%= @clock_entry.id %> a').hide().parent().append("<%= j render 'form', clock_entry: @clock_entry %>")
$('#edit-clock-entry-<%= @clock_entry.id %> form').hide().parent().append("<%= j render 'form', clock_entry: @clock_entry %>")